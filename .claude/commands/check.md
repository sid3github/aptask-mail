---
description: Run the full merge gate — lint, typecheck, unit tests. Use before claiming a task complete.
---

Run `npm run check` and report the result. If anything fails, identify the
first failure, propose a fix, and apply it. Then re-run. Repeat until green
or until you hit a blocker the user needs to resolve.
