---
description: Start a new spec doc for a feature. Usage:/spec <feature-slug>
---

You're starting a new spec for **$ARGUMENTS**.

1. Read `.claude/skills/spec-template.md` for the canonical format.
2. Create `docs/specs/$ARGUMENTS.md` using that template.
3. Fill out: Purpose, User Story, UX, API Contract, Provider Implications,
   AI Implications, Edge Cases, Test Plan, Out of Scope.
4. Ask the user for any unknowns.
5. Mark status as `draft`.

Do not write any implementation code in this turn. Only the spec.
